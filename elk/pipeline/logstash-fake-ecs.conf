input {
    kafka {
        bootstrap_servers => "kafka:9092"
        topics => ["fake-ecs"]
    }
}

filter {
    json {
        source => "message"
    }
}

output {
    elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "fake-ecs"
    }
    stdout {}
}
